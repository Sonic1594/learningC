#include <stdio.h>

/* constants are set with #define and persist in the file
they should also always be spelt with all caps to denonte
their scope
*/

#define MAX 100

int main() {
  printf("%d", MAX);
}
